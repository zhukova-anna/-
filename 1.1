#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "");
    int x,y;
    printf("x=");
    scanf("%d",&x);
    printf("y=");
    scanf("%d",&y);
    if((pow(x,2)+pow(y,2))<16&&x>0){
        if(y<0)
            printf("Точка належить");
        if(y>0&&(pow(x,2)+pow(y,2))>4)
            printf("Точка належить");
    }
    else
        printf("Точка не належить");
    return 0;
}
