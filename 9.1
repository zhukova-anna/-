#include <stdio.h>
#include <stdlib.h>

int main()
{
int i,n;
char *s = (char*)malloc(90*sizeof(char));
gets(s);
n=strlen(s);
	for (i=0;i<n;i++){
		if (s[i]==' ' && s[i+1]>96 && s[i+1]<123)
            {  s[i+1]=s[i+1]-32;
              printf("%c",s[i]);
             printf("%c",s[i+1]);
            i++; }
           else
        printf("%c",s[i]);
           }
    return 0;
}
