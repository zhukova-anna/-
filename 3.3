#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    srand(time(NULL));
    int i,j,n,k=0,h=0;
    printf("n= ");
    scanf("%d",&n);
    while(n<0){
        printf("Try again");
        scanf("%d",&n);
    }
    int V[n][n];
    for(i=0;i<n;i++)
        for(j=0;j<n;j++){
            V[i][j]=rand()%20;
    }
    for(i=0;i<n;i++){
        printf("\n");
        for(j=0;j<n;j++)
            printf("%2d ",V[i][j]);
    }
    int b[100];
    for(i=0;i<n;i++)
        for(j=0;j<n;j++){
                if(i>j)
                    h++;
                    }
    printf("\n");
        for(i=0;i<n;i++)
        for(j=0;j<n;j++){
            if(i>j){
                b[k]=V[i][j];
                k++;
        }
        }
    for(k=0;k<h;k++){
        printf("%d ",b[k]);
    }
    return 0;
}
