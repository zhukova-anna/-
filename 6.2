#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char str[100];
    gets(str);
    int n,i,id2=0;
    n=strlen(str);
    printf("Size: %d\n",n);
    int k=0;
    for (i=0;i<n;i++){
        if(str[i]=='a')
                k++;
    }
    printf("kilkist a: %d\n",k);

    int id1=0;
    for (i=0;i<n;i++){
        if (str[i]==' '){
            id1=i;
    break;
        }
    }
    for (i=id1;i<n;i++){
        if (str[i]==' ')
            id2=i+1;
    }
    for (i=id1;i<id2;i++){
        putchar(str[i]-32);
    }

    printf("\nkilkist bukv: %d", id1);
    return 0;
}
