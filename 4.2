#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    srand(time(NULL));
    int i,j,n,k=0,h=0;
    printf("n= ");
    scanf("%d",&n);
    while(n<0){
        printf("Try again");
        scanf("%d",&n);
    }
    int V[n][n];
    for(i=0;i<n;i++)
        for(j=0;j<n;j++){
            if(j%2==0&&i%2==0||j%2==1&&i%2==1)
            V[i][j]=1;
        else
            V[i][j]=0;
    }
    for(i=0;i<n;i++){
        printf("\n");
        for(j=0;j<n;j++)
            printf("%2d ",V[i][j]);
    }
    return 0;
}
